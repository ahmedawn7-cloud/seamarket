PROJECT: C:\Users\awnfx\seamarket


===== components/Dashboard.tsx =====

"use client";

import { useState, useMemo } from "react";
import { Search, Home, PackageSearch, Beaker, Truck, Users, User } from "lucide-react";
import ProductCard from "@/components/ProductCard";
import ProductDrawer from "@/components/ProductDrawer";
import HomeView from "@/components/HomeView";
import CommunityHub from "@/components/CommunityHub";
// import ResearchHub from "@/components/ResearchHub";
// import SourcingIntelligence from "@/components/SourcingIntelligence";
// import UserDashboard from "@/components/UserDashboard";

export default function Dashboard({ initialProducts }: { initialProducts: any[] }) {
  const [searchQuery, setSearchQuery] = useState("");
  const [drawerProduct, setDrawerProduct] = useState<any | null>(null);
  const [activeTab, setActiveTab] = useState("Home");

  // Safe fallback if initialProducts is undefined
  const safeProducts = Array.isArray(initialProducts) ? initialProducts : [];

  const filtered = useMemo(() => {
    if (!searchQuery.trim()) return safeProducts;
    const q = searchQuery.toLowerCase();
    return safeProducts.filter((p) => {
      const searchName = p.clean_name_ai || p.Clean_Name_AI || p.product_name || p.Product_Name || "";
      return searchName.toLowerCase().includes(q);
    });
  }, [safeProducts, searchQuery]);

  const tabs = [
    { id: "Home", icon: Home, label: "Home" },
    { id: "Products", icon: PackageSearch, label: "Products" },
    { id: "Research", icon: Beaker, label: "Research" },
    { id: "Sourcing", icon: Truck, label: "Sourcing" },
    { id: "Community", icon: Users, label: "Community" },
    { id: "Profile", icon: User, label: "Profile" },
  ];

  return (
    <>
      <div className="bg-mesh" />
      <div className="relative z-10 flex flex-col min-h-screen font-sans">
        
        {/* Navigation Bar */}
        <header className="sticky top-0 z-50 glass-card border-b border-gray-800/50 backdrop-blur-xl bg-black/40">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="flex items-center justify-between h-16 gap-8">
              <div className="flex items-center gap-3 shrink-0 cursor-pointer" onClick={() => setActiveTab("Home")}>
                <img src="https://saxtrrxaaahextpfskuq.supabase.co/storage/v1/object/public/assets/Profit%20Pilot%20AI.png" alt="Logo" className="h-8 w-auto" />
                <span className="text-xl font-bold text-white tracking-tight hidden sm:block">Profit Pilot AI</span>
              </div>

              <nav className="hidden md:flex flex-1 items-center gap-1 justify-center">
                {tabs.map((tab) => {
                  const Icon = tab.icon;
                  return (
                    <button key={tab.id} onClick={() => setActiveTab(tab.id)} className={`flex items-center gap-2 px-4 py-2 rounded-full text-sm font-medium transition-all ${activeTab === tab.id ? 'bg-indigo-500/10 text-indigo-400 border border-indigo-500/20' : 'text-gray-400 hover:text-gray-200 hover:bg-white/5'}`}>
                      <Icon className="h-4 w-4" /> {tab.label}
                    </button>
                  );
                })}
              </nav>

              <div className="relative w-64 shrink-0">
                <Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-gray-400" />
                <input type="text" placeholder="Search..." value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} className="w-full bg-black/20 border border-gray-700/50 text-white text-sm rounded-full pl-10 pr-4 py-2 focus:outline-none focus:border-indigo-500 transition-all" />
              </div>
            </div>
          </div>
        </header>

        {/* Dynamic Content */}
        <main className="flex-1 mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
          {activeTab === "Home" && <HomeView />}
          
          {activeTab === "Products" && (
            <div className="grid gap-6 grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
              {filtered.map((product, idx) => (
                <ProductCard key={`${idx}`} product={product} onResearch={(p: any) => setDrawerProduct(p)} onQuickView={(p: any) => setDrawerProduct(p)} />
              ))}
            </div>
          )}

          {activeTab === "Community" && <CommunityHub />}

          {/* Placeholders for tabs we haven't finalized in this folder yet */}
          {(activeTab === "Research" || activeTab === "Sourcing" || activeTab === "Profile") && (
            <div className="text-center py-20 text-gray-400 border border-dashed border-gray-700 rounded-xl bg-black/20">
              <h2 className="text-2xl font-bold mb-2">{activeTab} Terminal</h2>
              <p>Module integration in progress. UI components pending deployment.</p>
            </div>
          )}
        </main>
      </div>

      <ProductDrawer product={drawerProduct} onClose={() => setDrawerProduct(null)} />
    </>
  );
}


===== components/HomeView.tsx =====

"use client";
import WaitlistForm from "@/components/WaitlistForm";

export default function HomeView() {
  const VIDEO_URLS = [
    "https://saxtrrxaaahextpfskuq.supabase.co/storage/v1/object/public/assets/AliExpress_to_Shopee_transition_202606200115.mp4",
    "https://saxtrrxaaahextpfskuq.supabase.co/storage/v1/object/public/assets/E%20commerce.mp4",
    "https://saxtrrxaaahextpfskuq.supabase.co/storage/v1/object/public/assets/UI_dashboard_with_sales_growth_202606200115.mp4"
  ];
  
  const LOGO_URL = "https://saxtrrxaaahextpfskuq.supabase.co/storage/v1/object/public/assets/Profit%20Pilot%20AI.png";

  return (
    <div className="space-y-16 pb-20 px-4">
      {/* Header with Logo */}
      <section className="flex flex-col items-center justify-center gap-6 pt-10">
        <img src={LOGO_URL} alt="Profit Pilot AI" className="h-24 md:h-32 w-auto object-contain" />
        <h1 className="text-4xl md:text-6xl font-bold text-white text-center tracking-tight">
          Stop Guessing What Sells. <span className="text-indigo-500">Start Knowing.</span>
        </h1>
        <p className="text-gray-400 text-lg max-w-2xl mx-auto text-center">
          Live product intelligence across Shopee, Lazada & TikTok Shop.
        </p>
      </section>

      {/* Video Grid */}
      <section className="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto">
        {VIDEO_URLS.map((url, i) => (
          <div key={i} className="rounded-2xl overflow-hidden border border-gray-800 bg-black shadow-xl hover:border-indigo-500/50 transition-colors h-64">
            <video autoPlay loop muted playsInline preload="metadata" className="w-full h-full object-cover">
              <source src={url} type="video/mp4" />
            </video>
          </div>
        ))}
      </section>
      
      {/* Pricing Tiers */}
      <section className="max-w-5xl mx-auto">
        <h2 className="text-2xl font-bold text-center text-white mb-8">Pick your edge. Start free, upgrade when the data pays for itself.</h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {[
            { title: "Free (Guest)", price: "$0", perks: ["Top 10 products", "Basic articles", "Read-only access"] },
            { title: "Registered", price: "$0", perks: ["Top 25 products", "Save 1 product", "Basic analytics"] },
            { title: "Pro", price: "$9/mo", perks: ["Top 100 products", "Full AI Analytics", "Sourcing intelligence", "Custom alerts"] }
          ].map((tier, i) => (
            <div key={i} className={`p-8 border rounded-xl transition-all text-center ${tier.title === 'Pro' ? 'bg-[#1a1c29] border-indigo-500 shadow-lg shadow-indigo-500/10' : 'bg-[#13151f] border-gray-800 hover:border-gray-600'}`}>
              {tier.title === 'Pro' && <span className="text-[10px] uppercase tracking-widest text-indigo-400 font-bold bg-indigo-500/10 px-3 py-1 rounded-full mb-4 inline-block">Most Popular</span>}
              <h3 className="text-xl font-bold text-white">{tier.title}</h3>
              <p className="text-4xl font-bold text-indigo-400 my-6">{tier.price}</p>
              <ul className="space-y-3 text-gray-400 text-sm mb-8 text-left inline-block">
                {tier.perks.map((p, j) => <li key={j}>✓ {p}</li>)}
              </ul>
            </div>
          ))}
        </div>
      </section>

      {/* Waitlist Integration */}
      <section className="max-w-5xl mx-auto border-t border-gray-800/50 pt-16">
        <WaitlistForm />
      </section>
    </div>
  );
}


===== components/ProductDrawer.tsx =====

"use client";
import { X, Bookmark, Activity, TrendingUp, DollarSign, Package } from "lucide-react";

export default function ProductDrawer({ product, onClose, onResearch }: any) {
  if (!product) return null;

  const imageUrl = product.image_url || product.Image_URL;
  const displayName = product.clean_name_ai || product.Clean_Name_AI || product.product_name || "Unknown Product";
  
  // Simulated analytics data
  const trendScore = product.trend_rank || 85;
  const profitMargin = product.profit_score || 64;
  const compScore = 45; 

  return (
    <div className="fixed inset-0 z-[100] flex justify-end">
      {/* Backdrop */}
      <div className="absolute inset-0 bg-black/60 backdrop-blur-sm" onClick={onClose} />
      
      {/* Drawer Panel */}
      <div className="relative w-full max-w-2xl bg-[#0b0c13] border-l border-gray-800 h-full overflow-y-auto flex flex-col animate-in slide-in-from-right duration-300">
        
        {/* Header */}
        <div className="sticky top-0 bg-[#0b0c13]/90 backdrop-blur-md border-b border-gray-800 p-6 flex justify-between items-start z-10">
          <div>
            <p className="text-xs text-cyan-400 font-bold tracking-wider uppercase mb-1">Product Analytics Terminal</p>
            <h2 className="text-xl font-bold text-white leading-tight">{displayName}</h2>
          </div>
          <button onClick={onClose} className="p-2 text-gray-400 hover:text-white bg-[#13151f] rounded-lg transition-colors">
            <X className="h-5 w-5" />
          </button>
        </div>

        {/* Content */}
        <div className="p-6 space-y-8 flex-1">
          
          {/* Top KPI Grid */}
          <div className="grid grid-cols-4 gap-4">
            {[
              { label: "Trend Score", value: trendScore, icon: TrendingUp, color: "text-emerald-400" },
              { label: "Demand", value: "High", icon: Activity, color: "text-cyan-400" },
              { label: "Profit Score", value: profitMargin, icon: DollarSign, color: "text-indigo-400" },
              { label: "Risk Level", value: "Low", icon: Package, color: "text-amber-400" }
            ].map((kpi, i) => (
              <div key={i} className="bg-[#13151f] border border-gray-800 p-4 rounded-xl flex flex-col gap-2">
                <kpi.icon className={`h-5 w-5 ${kpi.color}`} />
                <div>
                  <p className="text-[10px] text-gray-500 font-bold uppercase tracking-wider">{kpi.label}</p>
                  <p className="text-lg font-bold text-white">{kpi.value}</p>
                </div>
              </div>
            ))}
          </div>

          {/* Image & Market Analysis */}
          <div className="grid grid-cols-2 gap-6">
            <div className="rounded-xl overflow-hidden border border-gray-800 bg-[#13151f] h-64 relative">
               {/* eslint-disable-next-line @next/next/no-img-element */}
               <img src={imageUrl} alt={displayName} referrerPolicy="no-referrer" className="w-full h-full object-cover opacity-80" />
            </div>
            <div className="space-y-4">
              <div className="bg-[#13151f] border border-gray-800 p-5 rounded-xl">
                <h4 className="text-sm font-bold text-white mb-2">AI Insights</h4>
                <p className="text-xs text-gray-400 mb-2"><strong className="text-gray-300">Why Trending:</strong> Strong short-form video demonstration potential.</p>
                <p className="text-xs text-gray-400 mb-2"><strong className="text-gray-300">Target Audience:</strong> Urban buyers, office workers.</p>
                <p className="text-xs text-gray-400"><strong className="text-gray-300">Risk:</strong> High saturation, expect copycats.</p>
              </div>
              <button className="w-full py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-bold rounded-xl flex items-center justify-center gap-2 transition-colors">
                <Bookmark className="h-4 w-4" /> Save to Watchlist
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}


===== package.json =====

{
  "name": "seamarket",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2",
    "next": "16.2.9",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.9",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}

